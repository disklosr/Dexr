<ContentControl x:Class="Dex.Uwp.Controls.Card"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:controls="using:Dex.Uwp.Controls"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:local="using:Dex.Uwp.Cards"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                d:DesignHeight="200"
                d:DesignWidth="400"
                mc:Ignorable="d">

    <ContentControl.Template>
        <ControlTemplate>
            <controls:ShadowHost MinHeight="48">
                <ContentPresenter Padding="{TemplateBinding Padding}">
                    <ContentPresenter.Background>
                        <SolidColorBrush Color="{ThemeResource AppGreyColorLighter3}" />
                    </ContentPresenter.Background>
                </ContentPresenter>
            </controls:ShadowHost>
        </ControlTemplate>
    </ContentControl.Template>
</ContentControl>